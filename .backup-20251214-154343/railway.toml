[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn api:app -b 0.0.0.0:${PORT:-8000}"
healthcheckPath = "/api/todays-board"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
