[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn api:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/todays-board"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
