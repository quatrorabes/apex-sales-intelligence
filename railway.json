{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python api.py",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
